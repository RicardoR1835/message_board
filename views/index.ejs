<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Document</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            background-color: #110e16;
            color: aliceblue;
            background-image: url('../static/img/thumbs-up-2056022_1920.jpg');
            background-size: cover;
        }

        h1 {
            width: 95%;
            display: inline-block;
        }

        .navname {
            padding-right: 25px;
            font-size: 20px;
        }

        .nava {
            padding-left: 10px;
        }

        .posts {
            height: 100%;
        }

        .sp {
            font-size: 15px;
            display: inline-block;
            width: 90%;
        }

        .d {
            color: red;
        }

        .like {
            color: green;
        }

        .unlike {
            color: red;
        }
    </style>
</head>

<body>
    <div class="fluid-container">
        <nav class="navbar navbar-light bg-dark justify-content-between">
            <a class="navbar-brand">The Rickbook</a>
            <form class="form-inline">
                <p class="navname">Welcome!</p>
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                <a class="nava" href='/logout'>Logout</a>
            </form>
        </nav>
        <div class="container inline-block">
            <div>
                <br>

                <form action="create" method="POST">
                    <label for="exampleFormControlTextarea1">What's on your mind?</label>
                        <input type="text" class="form-control col-4" name="name" placeholder="First name">
                        <br>
                    <textarea name="content" class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
                    <br>
                    <p class="text-right"><input type="submit" value="Post a message" class="btn btn-primary"></p>
                </form>
            </div>
            <div class="container-fluid">
                <% for (messages of data) {%>
                <div class="display">
                    <h5><%= messages.name %> - <%= messages.updatedAt %></h5>
                    <p class="sp"><%= messages.content %></p>
                </div>
                <div class="container-fluid">
                    <% for (i of messages.comments) { %>
                    <div class="container">
                        <h6><%= i.name %> - <%= i.updatedAt %></h6>
                        <p class="sp"><%= i.content %></p>
                    </div>
                    <% } %>
                    <form action="create_comment/<%= messages._id %>" method="POST">
                        <label for="exampleFormControlTextarea1">Post a comment:</label>
                        <input type="text" class="form-control col-4" name="name" placeholder="First name">
                        <br>
                        <textarea name="content" class="form-control" id="exampleFormControlTextarea1"
                            rows="2"></textarea>
                        <br>
                        <p class="text-right"><input type="submit" value="Post a comment" class="btn btn-primary"></p>
                        <br>
                    </form>
                </div>
                <% } %>
            </div>
        </div>



    </div>
</body>

</html>